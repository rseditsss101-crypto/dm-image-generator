services:
  - type: web
    name: dm-image-generator
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python app.py
